{{>layout/header}}
<h1>HomePage</h1>
<div class="col-md-12">
  <div class="row">
    <div class="col-md-12">
      <div class="left">
        <a href="post/savePost" role="button" class="btn btn-info active">post save</a>
        {{>layout/logout}}
      </div>
      {{^username}}
        <div class="right">
          {{>layout/oauth2}}
        </div>
      {{/username}}
      {{#username}}
        <div class="right">
          Login as : {{username}}
        </div>
      {{/username}}
    </div>
  </div>
</div>
</div>
{{>layout/footer}}