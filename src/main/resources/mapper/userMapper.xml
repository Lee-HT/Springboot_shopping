<?xml version="1.0" encoding = "UTF-8?"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shop.market.repository.UserMapper">
  <!--   yml alias 설정 -->
  <insert id="saveUser" parameterType="com.shop.market.dto.userD">
    insert into user_table(username, password, email, role)
    values(#{username}, #{password}, #{email}, ROLE_USER)
  </insert>

  <select id="findByUsername" parameterType="string" resultType="com.shop.market.dto.loginD">
    select username,password,email from user_table where username = #{username}
  </select>

  <update id="updateByUsername" parameterType="string">
    update user_table set password = #{password} where username = #{username}
  </update>
</mapper>